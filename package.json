{
  "scripts": {
    "compile": "globstar -- npx handlebars src/**/*.hbs -f  src/precompiled.js",
    "start": "npm run compile || cd server || node index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "globstar": "^1.0.0",
    "handlebars": "^4.7.8"
  }
}
